<Window x:Class="PromptHelper.Views.PromptEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prompt Editor"
        Width="780"
        Height="580"
        MinWidth="640"
        MinHeight="450"
        WindowStartupLocation="CenterOwner"
        AutomationProperties.Name="Prompt editor"
        Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock"
                   AutomationProperties.HeadingLevel="Level1"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,12"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                <TextBlock x:Name="HeadlineLabel"
                           Text="Headline"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text=" &lt;optional&gt;"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           Opacity="0.5"/>
            </StackPanel>

            <TextBox x:Name="HeadlineTextBox"
                     Grid.Row="1"
                     Style="{StaticResource ModernTextBoxStyle}"
                     AcceptsReturn="False"
                     AcceptsTab="False"
                     MaxLines="1"
                     AutomationProperties.LabeledBy="{Binding ElementName=HeadlineLabel}"
                     TextChanged="HeadlineTextBox_TextChanged"/>
        </Grid>

        <CheckBox x:Name="WrapLinesCheckBox"
                  Grid.Row="2"
                  Content="Wrap long lines"
                  Style="{StaticResource ModernCheckBoxStyle}"
                  Margin="0,0,0,10"
                  Checked="WrapLinesCheckBox_Changed"
                  Unchecked="WrapLinesCheckBox_Changed"
                  ToolTip="Changes only how the prompt is displayed in this editor. Saved prompt text is not modified."/>

        <TextBox x:Name="EditorTextBox"
                 Grid.Row="3"
                 Style="{StaticResource PromptEditorTextBoxStyle}"
                 AutomationProperties.Name="Prompt text"
                 Margin="0,0,0,16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton"
                    Content="Cancel"
                    Style="{StaticResource SecondaryButtonStyle}"
                    IsCancel="True"
                    Click="CancelButton_Click"
                    Margin="0,0,12,0"/>
            <Button x:Name="SaveButton"
                    Content="Save"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsDefault="True"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
