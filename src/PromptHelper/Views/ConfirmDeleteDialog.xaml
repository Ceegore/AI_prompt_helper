<Window x:Class="PromptHelper.Views.ConfirmDeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete Confirmation"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AutomationProperties.Name="Delete confirmation"
        Background="{DynamicResource AppBackgroundBrush}">
    <Border Background="{DynamicResource SurfaceBrush}" Padding="24" CornerRadius="12" Margin="16" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <StackPanel>
            <TextBlock x:Name="TitleTextBlock"
                       AutomationProperties.HeadingLevel="Level1"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,12"/>
            <TextBlock x:Name="MessageTextBlock"
                       FontSize="14"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,8"/>
            <TextBlock Text="This action cannot be undone."
                       FontSize="13"
                       Foreground="{DynamicResource DangerBrush}"
                       Margin="0,0,0,24"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Style="{StaticResource SecondaryButtonStyle}"
                        IsCancel="True"
                        Click="CancelButton_Click"
                        Margin="0,0,12,0"/>
                <Button x:Name="ActionButton"
                        Content="Delete"
                        Style="{StaticResource DangerButtonStyle}"
                        IsDefault="True"
                        Click="ActionButton_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
